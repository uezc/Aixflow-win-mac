# 复制为同目录 secrets.toml 后填写真值（secrets.toml 已在 .gitignore，勿提交）

admin_password = "请修改"
ots_access_key_id = ""
ots_access_key_secret = ""
# 可选：兑换码表名，默认 nx_coupons
# ots_table_coupons = "nx_coupons"

# 官方邮件（Resend）；也可放在仓库根目录 .env 的 RESEND_API_KEY / RESEND_FROM_EMAIL
# resend_api_key = "re_xxxx"
# resend_from_email = "AIXFLOW <auth@aixflow.ai>"
