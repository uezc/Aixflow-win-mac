---
description: 短剧分镜节奏硬规则：对话细切、赶路粗切，防止场景太多对话太少
globs: src/shared/directorDomain/**/*.{ts,tsx},src/shared/storyboardScript/**/*.{ts,tsx},src/renderer/components/DirectorStudio/**/*.{ts,tsx}
alwaysApply: false
---

# 短剧快节奏规则（强制执行）

修改或生成短剧分镜（shot plan / 分镜脚本）时必须遵守：

## 1. 场景优先级

- 有对白的场景（对话/对峙/谈判）→ 分配 **50% 以上** 的镜头数（含说话人镜 + 反应镜）
- 无对白的场景（赶路/环境/过渡）→ 分配 **30% 以下** 的镜头数
- 禁止为凑镜头数拆解赶路动作

## 2. 对话切镜规则

- 每 **2～3 句** 有信息量的对白必须切一次镜
- 每段对话必须「说话人镜头 + 听话人反应镜头」交替
- 关键情绪动作（攥拳又松开 / 喉结动 / 闭眼 / 点屏）必须 **单独成镜**
- **禁止**把整场 8～10 句对白压进 1 镜（含「镜内极速正反打」时间轴写法）

## 3. 砍场景规则

- 同一人物的同一赶路动作最多 1～2 镜
- 电梯/走廊/门口/大堂等过渡空间能合并就合并
- 只保留：建立环境、人物出场、对话核心、情绪收尾

## 4. 总镜数控制

- 1 分钟级短剧 → 约 10～15 镜
- 平均每镜偏短（优先 6 秒档）
- 实现入口：`src/shared/directorDomain/prompts/shotPlan.ts` + `shotPlanValidate.ts`

## 5. 拍摄执行表 QA（Decomposition）

- 威胁源/说话人切换必须有桥接镜或时长标注（离开 / 拒之门外 / 跳切 N 秒）
- 雨夜车内等极端天气：默认雨声 Loop + 对白 ADR，不写「同期可收」
- 短时特写：每镜 1 个主微相，禁止微表情堆砌
- 悬念结尾：必须标注「留悬念接下一场」
