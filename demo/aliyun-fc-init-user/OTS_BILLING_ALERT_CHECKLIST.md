# 阿里云费用告警配置清单（OTS 止血后）

配合 2026-09-08 事故复盘。代码侧已止血；以下在**控制台**完成即可，无需再改 FC。

## 1. 余额多档预警

路径：费用中心 → 预警设置 / 余额预警

建议阈值（短信+邮件）：

| 阈值 | 用途 |
|---|---|
| ¥200 | 提前预警 |
| ¥100 | 加强关注 |
| ¥50 | 紧急，暂停压测/大清理 |

可选：开通自动充值（若可接受）。

## 2. OTS 用量/费用告警（香港 `nexflow-db`）

路径：云监控 / 费用中心 → 按产品表格存储 → 实例 `nexflow-db`（地域：中国香港）

建议告警（任一触发即可）：

| 指标 | 阈值 | 说明 |
|---|---|---|
| 小时应付费用 | **> ¥1** | 正常空闲约 ¥0.1～0.2/h；¥1 已异常 |
| 下行流量 | **> 100 MB/h** | 旧事故约 11 GB/h |
| 按量读 CU | 相对基线突增 | 可选 |

北京 `search_index` 预留读约 ¥0.01/h，属固定成本，勿与事故混淆。

## 3. 运维红线（人工）

- **禁止**重新部署 `nexflow-fc-2026-09-07T10-14-37-153Z` 旧包
- **禁止**默认开启 `NX_TASK_WORK_FALLBACK_ON_EMPTY=1`
- 大批量 cleanup / Admin 全表扫：先 DRY_RUN，确认有 `columnToGet`
- 项目规则已写入：`.cursor/rules/ots-read-redlines.mdc`

## 4. 建议观察窗口

部署后连续看 **2～3 天**：

- 阿里云余额是否按约 ¥2～5/天缓降（空闲）
- SLS Timer：`idle_no_patrol=1` 且 `ms` 亚秒
- `video.running` / `image.running` 是否长期保持 0（无真实任务时）；计数漂移会让 Timer 误判有活
- Timer 建议 `@every 5m`；`NX_QUEUE_IDLE_PROBE_MS=900000`（15min）；`NX_QUEUE_IDLE_FULL_SWEEP_MS=10800000`（3h）
